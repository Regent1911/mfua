# Инструкция по использованию PowerShell скрипта синхронизации репозиториев

## 📌 Назначение скрипта

Данный PowerShell скрипт предназначен для автоматической синхронизации файлов между двумя Git-репозиториями:

- **Донор** (`from_rurewa/mfua`) - источник изменений
- **Реципиент** (`local_rurewa/mfua`) - целевой репозиторий, куда копируются изменения

Скрипт выполняет:

1. Клонирование репозиториев (при их отсутствии локально)
2. Обновление репозитория-донора
3. Копирование файлов из донора в реципиент с сохранением структуры
4. Создание коммита с автоматическим сообщением
5. Отправку изменений на удаленный сервер

## 🛠️ Предварительные требования

### 1. Установка Git

Скрипт требует наличия установленного Git:

- Скачайте с официального сайта: <https://git-scm.com/>
- Во время установки выберите опцию **"Git from the command line and also from 3rd-party software"** или **"Add Git to PATH"**
- Проверить установку: откройте PowerShell и введите `git --version`

### 2. Настройка PowerShell

**Разрешение на выполнение скриптов:**

PowerShell по умолчанию ограничивает выполнение скриптов. Для первого запуска выполните в PowerShell от имени администратора:

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser