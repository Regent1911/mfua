# Инструкция по использованию скрипта синхронизации репозиториев

## 📌 Назначение скрипта

Данный скрипт предназначен для автоматической синхронизации файлов между двумя Git-репозиториями:

- **Донор** (`from_rurewa/mfua`) - источник изменений
- **Реципиент** (`local_rurewa/mfua`) - целевой репозиторий, куда копируются изменения

Скрипт выполняет:

1. Клонирование репозиториев (если их нет локально)
2. Обновление репозитория-донора
3. Копирование файлов из донора в реципиент
4. Создание коммита с изменениями
5. Отправку изменений на удаленный сервер

## 🛠️ Предварительные требования

### 1. Установка Git

Скрипт требует наличия установленного Git:

- Скачайте с официального сайта: <https://git-scm.com/>
- Во время установки выберите опцию **"Add Git to PATH"**
- Проверить установку: откройте командную строку и введите `git --version`

### 2. Настройка окружения

**Важно**: Перед первым использованием скрипта проверьте:

- Доступ к удаленным репозиториям (GitHub и GitFlic)
- Наличие прав на запись в целевую директорию

## ⚙️ Настройка параметров

Перед запуском отредактируйте настройки в начале скрипта (строки 6-10):

```batch
set "BASE_DIR=D:\_Regent\Education"      ; Базовый каталог для репозиториев
set "DONOR_DIR=from_rurewa\mfua"         ; Подкаталог донора
set "RECIP_DIR=local_rurewa\mfua"        ; Подкаталог реципиента
set "DONOR_URL=https://gitflic.ru/project/rurewa/mfua"   ; URL донора
set "RECIP_URL=https://github.com/Regent1911/mfua"       ; URL реципиента
```

### Параметры для настройки

- **BASE_DIR**: Путь к папке, где будут храниться оба репозитория
- **DONOR_DIR**: Имя папки репозитория-донора
- **RECIP_DIR**: Имя папки репозитория-реципиента
- **DONOR_URL**: URL для клонирования репозитория-донора
- **RECIP_URL**: URL для клонирования репозитория-реципиента

## 🚀 Запуск скрипта

### Способ 1: Двойной клик

1. Сохраните файл с расширением `.bat` (например, `sync_repos.bat`)
2. Дважды щелкните по файлу

### Способ 2: Через командную строку

```cmd
cd путь_к_скрипту
sync_repos.bat
```

## 🔄 Процесс работы

Скрипт выполняет следующие шаги:

### Шаг 1: Проверка каталогов

- Проверяет наличие Git в системе
- Проверяет существование локальных копий репозиториев
- При отсутствии - клонирует репозитории с удаленных серверов

### Шаг 2: Обновление репозитория-донора

- Переходит в каталог донора
- Выполняет `git fetch` и `git pull`
- Пытается обновить ветку `master`, затем `main`

### Шаг 3: Копирование файлов

- Копирует все файлы и папки (кроме `.git`) из донора в реципиент
- Использует `xcopy` для рекурсивного копирования папок

### Шаг 4-7: Работа с Git реципиента

1. Переходит в каталог реципиента
2. Добавляет все изменения: `git add .`
3. Создает коммит с автоматическим сообщением, содержащим дату и время
4. Пытается отправить изменения на GitHub (ветки `master` или `main`)

## ⚠️ Особенности и ограничения

### Что копируется

- Все файлы из корневой директории донора
- Все подпапки (рекурсивно), кроме папки `.git`

### Что не копируется

- Файлы, начинающиеся с точки (кроме явно указанных)
- Скрытые файлы и системные файлы
- История Git (папка `.git`)

### Конфликты и перезапись

- Существующие файлы в реципиенте будут **перезаписаны**
- Удаленные файлы в доноре **не удаляются** из реципиента
- Рекомендуется сделать бэкап перед первым запуском

## 🐛 Возможные проблемы и решения

### 1. "Git не установлен"

```
ОШИБКА: Git не установлен или не добавлен в PATH
```

**Решение**: Установите Git и добавьте его в переменную PATH

### 2. Ошибки клонирования

```
ОШИБКА: Не удалось клонировать репозиторий
```

**Решение**:

- Проверьте интернет-соединение
- Убедитесь в правильности URL репозиториев
- Проверьте права доступа к репозиториям

### 3. Конфликты при пуше

```
ВНИМАНИЕ: Не удалось отправить изменения
```

**Решение**:

- Вручную выполните `git pull --rebase` в каталоге реципиента
- Разрешите конфликты
- Запустите скрипт повторно

### 4. Проблемы с кодировкой

**Решение**: Скрипт использует кодировку UTF-8 (chcp 65001)

## 📝 Ручное выполнение операций

Если скрипт завершился с ошибкой, вы можете выполнить команды вручную:

```cmd
cd /d "D:\_Regent\Education\local_rurewa\mfua"
git status                      # Проверить изменения
git add .                       # Добавить все файлы
git commit -m "Сообщение"       # Создать коммит
git pull origin master --rebase # Обновить с rebase
git push origin master          # Отправить изменения
```

## 🔒 Безопасность

- Скрипт не запрашивает пароли (использует сохраненные учетные данные Git)
- Не изменяет системные файлы
- Работает только в указанной директории
- Перед копированием рекомендуется проверить список файлов

## 📁 Структура после выполнения

```
D:\_Regent\Education\
├── from_rurewa\mfua\      # Репозиторий-донор
│   ├── .git\
│   └── [файлы проекта]
└── local_rurewa\mfua\     # Репозиторий-реципиент
    ├── .git\
    └── [скопированные файлы]
```

## 💡 Советы по использованию

1. **Первый запуск**: Убедитесь, что оба репозитория доступны
2. **Регулярное использование**: Запускайте скрипт при необходимости синхронизации
3. **Мониторинг**: Проверяйте вывод скрипта на наличие предупреждений
4. **Резервное копирование**: Перед первым запуском сделайте бэкап целевого репозитория

## 📄 Лицензия и авторство

Скрипт предназначен для внутреннего использования.
Автоматически сгенерированные коммиты содержат метку времени.

---
*Примечание: Для остановки скрипта во время выполнения нажмите `Ctrl+C`*
